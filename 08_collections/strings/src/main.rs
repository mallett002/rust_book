fn main() {
    // Create Strings
    let mut s = String::new();

    // with initial data
    let data = "initial contents";
    let s = data.to_string();
    let s = "hello".to_string();
    let s = String::from(data);

    // TODO: left off https://doc.rust-lang.org/book/ch08-02-strings.html#listing-8-12
}
